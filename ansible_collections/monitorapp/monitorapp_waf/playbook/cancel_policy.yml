- hosts: all
  remote_user: root
  gather_facts: yes
  tasks:
  - name: Cancel policy changes
    cancel_policy:
      db_id: "{{ db.id }}"
      db_password: "{{ db.password }}"
    register: result
  - debug: var=result
