- hosts: all
  remote_user: root
  gather_facts: yes
  tasks:
  - name: Request time sync
    time:
      db_id: "{{ db.id }}"
      db_password: "{{ db.password }}"
    register: result
  - debug: var=result
